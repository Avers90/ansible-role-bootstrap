---
# Note: This role uses 'raw' module which runs before facts are gathered,
# so we cannot check ansible_distribution. It works on Debian/Ubuntu systems
# that use apt-get for package management.

- name: bootstrap | Check if Python is installed
  ansible.builtin.raw: command -v python3
  register: bootstrap_python_check
  changed_when: false
  failed_when: false

- name: bootstrap | Install Python and dependencies
  ansible.builtin.raw: apt-get update && apt-get install -y {{ bootstrap_python_packages | join(' ') }}
  when: bootstrap_python_check.rc != 0
  changed_when: true

- name: bootstrap | Gather facts after Python is available
  ansible.builtin.setup:
